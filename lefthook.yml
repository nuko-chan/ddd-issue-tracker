# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
pre-push:
  jobs:
    - name: packages audit
      run: pnpm run tsc

pre-commit:
  parallel: true
  jobs:
    - run: pnpm check
      glob: "*.{js,ts,jsx,tsx,md}"
